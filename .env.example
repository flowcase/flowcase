# Flowcase Environment Variables
# Copy this file to .env and fill in the values

# Domain Configuration
# Your domain name (e.g., example.com or localhost for local development)
DOMAIN=example.com

# Traefik Configuration
# Email address for Let's Encrypt certificate notifications
ADMIN_EMAIL=admin@example.com

# ACME Certificate Authority Server
# Use "https://acme-v02.api.letsencrypt.org/directory" for production
# Use "https://acme-staging-v02.api.letsencrypt.org/directory" for testing
# Leave empty or use staging for local development
CA_SERVER=https://acme-staging-v02.api.letsencrypt.org/directory

# PostgreSQL Configuration
# Database password (REQUIRED)
PG_PASS=your-secure-password-here

# Optional: PostgreSQL user (defaults to "authentik")
# PG_USER=authentik

# Optional: PostgreSQL database name (defaults to "authentik")
# PG_DB=authentik

# Authentik Configuration
# Secret key for Authentik (REQUIRED)
# Generate a secure random key using: openssl rand -base64 32
AUTHENTIK_SECRET_KEY=your-secret-key-here

# Optional: Authentik image and tag
# AUTHENTIK_IMAGE=ghcr.io/goauthentik/server
# AUTHENTIK_TAG=2025.6.4
